function RandomNumberButton() {
  return (
    <button
      style={{
        backgroundColor: "#000",
        border: "none",
        borderRadius: "8px",
        color: "#fff",
        cursor: "pointer",
        padding: "8px 24px",
      }}
    >
      Ger random number
    </button>
  );
}

export default RandomNumberButton;
